#!/bin/sh
if [ "$#" -eq 1 ] && [ "$1" = "secret-tool" ]; then
    if [ "${EPHEMERA_KEYRING_TEST:-0}" = "1" ]; then
        exit 0
    fi
    exit 1
fi
printf 'unexpected test which invocation: %s\n' "$*" >&2
exit 2
